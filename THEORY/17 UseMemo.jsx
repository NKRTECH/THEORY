// React Memoization Example
// In React, we can optimize our application by avoiding unnecessary component re-render using memoization.
// As I mentioned too in this other article about managing state in React, components re-render because of two things: 
// a change in state or a change in props. This is precisely the information we can "cache" to avoid unnecessary re-renders.
// But before we can jump to the code, let's introduce some important concepts.